# Visual EQ NpcViewer

## Run instructions

* Copy graphics folder from visual-eq
* Run `yarn`
* Run `yarn build`
* Run `yarn serve`

## Requirements

* Node LTS (10.6+)
* yarn (`npm install --global yarn`)